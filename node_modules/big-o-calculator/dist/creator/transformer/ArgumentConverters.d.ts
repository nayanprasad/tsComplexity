import { Language } from '../../structures';
declare function convertString(argument: string): string;
declare function convertArray(argument: Array<number | string>, language: Language): string;
export { convertString, convertArray };
