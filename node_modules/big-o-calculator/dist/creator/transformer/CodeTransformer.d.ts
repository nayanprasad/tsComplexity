import { Code, Language, RunnableCode, TestSample } from '../../structures';
declare type CodeTransformerFunction = (code: Code, sample: TestSample) => string;
declare class CodeTransformer {
    static readonly REPLACEMENT_PATTERN: string;
    private languageTransformers;
    private languagePatterns;
    constructor();
    private createRunnableCode;
    private applyLanguageSpecialRules;
    private argumentsInLanguageSyntax;
    addLanguageCodeTransformer(language: Language, transformer: CodeTransformerFunction): void;
    addLanguageReplacePattern(language: Language, pattern: string | RegExp): void;
    injectFunctionArguments(code: Code, samples: TestSample[]): RunnableCode[];
}
export { CodeTransformer, CodeTransformerFunction };
