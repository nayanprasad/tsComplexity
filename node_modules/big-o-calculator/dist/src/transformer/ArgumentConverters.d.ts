import { Language } from "../structures";
declare function convertString(argument: string, language: Language): string;
declare function convertArray(argument: Array<any>, language: Language): string;
export { convertString, convertArray };
