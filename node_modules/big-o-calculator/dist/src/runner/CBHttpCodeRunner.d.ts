import { CodeRunner } from "./CodeRunner";
import { HttpClient } from "./HttpClient";
import { Language } from "../structures";
declare type ResponseHandler = (stdout: string) => number;
declare class CBHttpCodeRunner implements CodeRunner {
    static readonly URI_PATTERN_LANG_PART = "[runnerLanguage]";
    private uriPattern;
    private client;
    private languageUris;
    private languageResponseHandlers;
    constructor(uri: string, client: HttpClient);
    addResponseHandler(language: Language, handler: ResponseHandler): void;
    checkCodeExecDuration(language: Language, code: string, context?: {
        [key: string]: any;
    }): Promise<number>;
    private determineRunnerUri;
    private createCodeSampleBody;
}
export { CBHttpCodeRunner, ResponseHandler };
